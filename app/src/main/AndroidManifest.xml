<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._2412501591_Tugas06_AH">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false"/>
        <activity android:name=".PersegiPanjangActivity" />
        <activity android:name=".BujurSangkarActivity" />
        <activity android:name=".SegitigaActivity" />
        <activity android:name=".LingkaranActivity" />
        <activity android:name=".TrapesiumActivity" />
        <activity android:name=".JajarGenjangActivity" />

    </application>

</manifest>